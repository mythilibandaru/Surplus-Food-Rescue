export type Role = "Donor" | "NGO" | "Volunteer" | "Admin";
export type DonationStatus = "Available" | "Accepted" | "PickedUp" | "Delivered" | "Completed" | "Expired";
export interface User { ... }
export interface Donation { ... }
export interface Notification { ... }
